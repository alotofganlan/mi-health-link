# mi-health-link 项目现状

> 更新日期：2026-09-19
> 当前定位：单用户、自托管的个人健康数据同步与分析后端。它不是小米官方项目，也不是医疗器械。

## 这个项目解决什么问题

mi-health-link 从小米运动健康云端读取个人健康数据，将已验证的数据归一化保存到自己的 Supabase，再通过受保护的 MCP 接口提供给 ChatGPT。项目还包含睡醒触发晨报、天气与周期提醒、Nightscout 血糖同步，以及若干用于验证小米接口行为的诊断工具。

推荐的使用方式是：每位使用者拥有独立的 VPS、Supabase 项目、小米账号凭据和消息渠道。当前版本不支持多人共用同一个部署。

## 数据流概览

1. VPS 使用本地保存的小米会话访问小米健康云。
1. 普通同步把支持的指标直接写入归一化表；部分诊断和特殊端点仍可保存原始响应。
2. MCP 服务使用 Supabase OAuth 验证访问令牌，并限制为一个明确允许的用户。
3. ChatGPT 通过 MCP 按需读取健康数据、数据覆盖状态和晨报快照。
4. 手机 Automate 可在解锁后通知 VPS；VPS 判断是否存在新的睡眠，再通过 Slack 发送只包含报告编号的触发消息。

## 已经完成的功能

### 小米账号与同步

- 交互式小米账号登录，以及 serviceToken、ssecurity、passToken 的本地保存。
- passToken 自动刷新过期的小米健康会话。
- 支持中国、新加坡、美国、德国、俄罗斯等小米健康区域。
- 只把小米实际返回过的健康键加入发现注册表，避免把猜测的字段名当成真实数据。
- 增量同步、历史回填、分页、检查点、失败状态和数据覆盖区间。
- 初次回填策略：睡眠最多 365 天、经期尽可能读取全部历史、普通指标默认最近 30 天。
- 手表睡眠优先于手机睡眠，避免同一天混合两种来源。

### 已归一化或可读取的数据

当前代码覆盖的主要类别包括：

- 睡眠及睡眠阶段；
- 步数、距离、热量、中高强度活动、有效站立；
- 心率、静息心率、血氧、压力、异常心搏事件；
- 体温趋势、单次体温、体温特征；
- 体重和部分身体成分；
- 锻炼记录、训练负荷、活力值、PAI 和跑步能力相关记录；
- 经期边界、经期症状和基础周期预测；
- 饮食、餐次与食物明细；
- 单点血糖、Nightscout 数据和实验性的连续血糖镜像。

并非每个小米账号、设备或地区都会返回全部指标。接口返回“支持”不代表数据源一定有记录。

### MCP 与访问控制

已经提供以下 MCP 能力：

- 列出可用指标；
- 查询指定时间范围的健康数据；
- 一次读取多项指标及服务端摘要；
- 查询数据覆盖状态；
- 启动最近同步或指定区间回填，并查询任务状态；
- 使用 report_id 读取固定的晨报上下文；
- 预览并在明确确认后修复缺失的中高强度活动分钟。

MCP 使用 Supabase JWT、签名密钥、issuer、audience、scope 和单一 subject 白名单进行校验。健康数据接口不是匿名公开接口。

### 晨报链路

- 手机解锁和位置更新使用两个独立的 Bearer 鉴权接口。
- 解锁接口立即返回 202，较慢的小米同步在后台运行，避免 Automate 因超时停止。
- 系统会区分主睡眠、午睡、睡眠更新和新的睡眠记录。
- 同一条逻辑睡眠只触发一次报告。
- 每份报告使用唯一 report_id，并保存不可变的上下文快照。
- 晨报快照可包含睡眠、昨日健康、7 天与 30 天趋势、天气、基础周期预测和数据覆盖说明。
- Slack 可使用用户 OAuth 令牌以用户身份发送触发消息；旧 Incoming Webhook 仍作为未配置用户令牌时的兼容方式。

### 周期与提醒

- 已实现与指定小米运动健康 APK 基础日历规则兼容的经期长度、经期范围、排卵日和易孕期计算。
- 已实现经期开始前 7 天提醒字段。
- 已提供 6 个月、12 个月和全部历史的周期统计。
- 所有周期结果仅供健康记录参考，不用于避孕、诊断或治疗。

### 受保护的写入工具

- 支持手工血糖和实验性连续血糖数据写入小米云，并进行回读或状态校验。
- 支持根据心率阈值预览缺失的中高强度活动分钟；只有显式确认后才写入，并在写入后回读验证。
- 这些工具可能修改云端健康数据，不应在不了解影响时运行。

### 测试和部署

- 当前测试套件覆盖同步、归一化、OAuth、MCP、晨报、位置隐私、饮食、锻炼、血糖和强度修复等路径。
- 仓库包含 systemd 示例、GitHub Actions 测试与 VPS 部署流程。
- 仓库包含基础建表 SQL、按顺序执行的增量迁移，以及从全新 Supabase 和 VPS 开始的操作指南。
- 现有部署流程仍带有本项目开发环境的历史假设，不应直接视为通用安装器。

## 部分完成或存在已知问题

### 晨报渲染仍不够稳定

get_morning_context 已经能返回完整健康数据，但当前 Slack 提示语可能让模型把 partial 或 sync_failed 的覆盖状态误解为“接口没有数据”。正确含义应是：已有值可以报告，同时注明覆盖可能不完整。这个解释规则和紧凑的 report_brief 仍待实现。

### 日历尚未接入

晨报结构已经预留 Structured 和小米日历来源，但当前状态固定为 not_connected。今明后日程、对话创建提醒以及手机日历上传尚未完成。

### 恢复时间尚未标准化

锻炼响应中的 recover_time 原始代码已经保存在 recover_time_code，但单位和含义尚未正式确认，因此 recovery_min 仍为空。项目不会擅自把原始代码解释成分钟或小时。

### 原始数据尚未完全退出

普通健康同步已经优先直接写入归一化表，但特殊端点、手工 probe、历史发现注册表和旧数据库仍可能使用 raw_records。仓库没有自动删除旧原始数据的迁移。

### 从零部署尚待独立环境验证

仓库已经补充基础表、索引、权限收紧迁移和逐步操作指南。相关结构有静态契约测试，但尚未在一套全新的外部 Supabase 项目和 VPS 上完成端到端演练，因此仍可能遇到平台界面变化、系统包差异或 OAuth 客户端配置问题。

### 部署工作流仍偏向开发者环境

部分 GitHub Actions 只监听历史功能分支，主部署工作流默认面向一个名为 vps 的 GitHub Environment。公开模板需要改成手工启用或提供明确的仓库变量说明。

## 已设计但尚未实现

以下内容出现在设计文档中，但不能视为当前产品能力：

- 基于个人基线、通用参考范围和设备标记的完整异常检测引擎；
- 持久化健康告警、去重、升级和自托管 ntfy 一键同步操作；
- 睡眠、白天、锻炼、周期和长期趋势的交互式健康 UI；
- 数据库容量预测、免费额度预警和自动化数据保留策略；
- 自动删除或导出个人数据的完整生命周期工具；
- 使用体温和模型推断的高级经期预测；当前没有复现 APK 的 GRU 智能预测；
- 真正的日历聚合和任务写入；
- 多用户、租户隔离、共享 VPS 上的账号分区；
- 经过验证并标准化的锻炼恢复时长。

## 适合与不适合的使用场景

适合：

- 熟悉 VPS、Python、Supabase 和环境变量的个人；
- 愿意自行管理小米账号凭据和健康数据的人；
- 用于个人研究、自动化和非医疗用途。

不适合：

- 多人共用同一部署；
- 医疗诊断、紧急告警或避孕决策；
- 不愿意把位置发送给天气和地理编码服务的用户；
- 期待开箱即用、无需维护的消费者应用。

## 下一阶段建议

项目已经使用清理后的工作树建立独立仓库，不包含旧 Git 历史，并采用 MIT License。下一阶段建议依次完成：

1. 在全新 Supabase 项目上实际演练并复核现有建表与权限脚本。
2. 将 OAuth 页面的第三方 CDN SDK 改成本地构建资源。
3. 完成晨报 report_brief 和覆盖状态解释规则。
4. 在全新 VPS 上执行部署指南并记录平台或系统版本差异。
5. 在 GitHub Actions 中增加持续的秘密扫描。

更多隐私边界和公开检查项见 PRIVACY.md。
